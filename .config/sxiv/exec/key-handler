#!/bin/sh
while read file
do
  case "$1" in

  # Rotate 90 degrees clockwise
  "C-r")
    convert -rotate 90 "$file" "$file" ;;

  "C-s")
    share "$file" ;;

  "C-e")
    gimp "$file" ;;

  esac
done
