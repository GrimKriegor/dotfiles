#!/bin/sh
while read file
do
  case "$1" in

  # Edit
  "C-e")
    gimp "$file" ;;

  # Rotate 90 degrees
  "C-r")
    convert -rotate 90 "$file" "$file" ;;

  # Share
  "C-s")
    share "$file" ;;

  esac
done
